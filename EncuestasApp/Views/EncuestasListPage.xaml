<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:EncuestaApp.Views"
             x:Class="EncuestaApp.Views.EncuestasListPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#F5F5F5">

    <!-- Grid principal: contenido | navbar -->
    <Grid RowDefinitions="*, 70">

        <!-- ===== CONTENIDO PRINCIPAL ===== -->
        <Grid Grid.Row="0">
            <ScrollView>
                <VerticalStackLayout Padding="20" Spacing="10">

                    <Button Text="Nueva Encuesta"
                            Clicked="OnNuevaEncuestaClicked"
                            BackgroundColor="#055A1C"
                            TextColor="White"/>

                    <Button x:Name="SyncButton"
                            Text="🔄 Sincronizar"
                            Clicked="OnSyncClicked"
                            BackgroundColor="Gray"
                            TextColor="White"/>

                    <Label Text="Lista de encuestas guardadas de la brigada"
                           FontAttributes="Bold"
                           Margin="0"
                           FontSize="16"/>

                    <CollectionView x:Name="EncuestasCollection"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame CornerRadius="10" Padding="10" Margin="5">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding NombreCompleto}" FontAttributes="Bold" FontSize="16"/>
                                        <Label Text="{Binding Edad, StringFormat='Edad: {0} años'}"/>
                                        <Label Text="{Binding Genero, StringFormat='Género: {0}'}"/>
                                        <Label Text="{Binding DomicilioCompleto, StringFormat='Domicilio: {0}'}"
                                               LineBreakMode="TailTruncation"/>
                                    </VerticalStackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                </VerticalStackLayout>
            </ScrollView>

            <!-- Overlay de sincronización (se mantiene dentro del contenido) -->
            <Grid x:Name="SyncOverlay"
                  IsVisible="False"
                  BackgroundColor="#80000000"
                  InputTransparent="False">
                <VerticalStackLayout VerticalOptions="Center"
                                     HorizontalOptions="Center"
                                     Spacing="15">
                    <ActivityIndicator IsRunning="True"
                                       Color="White"
                                       WidthRequest="50"
                                       HeightRequest="50"/>
                    <Label x:Name="SyncStatusLabel"
                           Text="Sincronizando..."
                           TextColor="White"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>
            </Grid>
        </Grid>

        <!-- ===== NAVBAR REUTILIZABLE ===== -->
        <views:BottomNavBar Grid.Row="1" ActiveTab="home"/>

        <!-- Fondo verde detrás del botón + -->
        <Grid Grid.Row="1"
      HorizontalOptions="Center"
      VerticalOptions="Start"
      WidthRequest="70"
      HeightRequest="35"
      BackgroundColor="#1B5E20"/>

        <!-- Botón + flotante -->
        <Border Grid.Row="1"
        WidthRequest="60" HeightRequest="60"
        HorizontalOptions="Center"
        VerticalOptions="Start"
        Margin="0,-20,0,0"
        BackgroundColor="#4CAF50"
        StrokeShape="RoundRectangle 30"
        Stroke="White"
        StrokeThickness="3">
            <Label Text="+"
           FontSize="32"
           FontAttributes="Bold"
           TextColor="White"
           HorizontalOptions="Center"
           VerticalOptions="Center"
           Margin="0,-3,0,0"/>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnAddTapped"/>
            </Border.GestureRecognizers>
        </Border>

    </Grid>
</ContentPage>